<style type="text/css">
  form {
    padding-bottom: 10px;
  }
  .order-view{
    border: 2px solid Black;
    margin: 5px;
  }
</style>


<p id="notice"><%= notice %></p>

<h1>Orders ( <%= @customer.customer_name  %> )</h1>
<a href="<%= new_store_customer_order_path(@store,@customer) %>"><button class="btn btn-xs btn-primary has-icon">New Order</button></a> || <a href="<%= store_customers_path(@store) %>"><button class="btn btn-xs btn-primary has-icon">Back</button></a>

<!--<%= link_to 'New Order', new_store_customer_order_path(@store,@customer) %>
<%= link_to 'Back', store_customers_path(@store) %> -->

<div class="container">
  <div class="row">
    
    <% @orders.each do |order| %>

      <div class=" order-view card shadow col-12 col-sm-6 col-md-4 col-lg-3">
        
        <a href="<%=store_customer_order_path(@store,@customer,order)%>"><strong><i>Order No : <%= order.id %></i></strong></a><br>
        <b>Trail Date :</b> <%= order.trail_date %> Status : <%= order.trail_status %> <br>
        <b>Delivery Date :</b> <%= order.delivery_date %> Status: <%= order.delivery_status %>
        

      </div>
      
    <% end %>


  </class>
</class>

