json.extract! store, :id, :store_name, :contact_person_name, :address, :contact_number, :email, :facebook_link, :whatsapp_number, :youtube_link, :instagram_link, :website, :logo, :created_at, :updated_at
json.url store_url(store, format: :json)
